<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatis.mapper.UserMapper">

    <resultMap id="getUser" type="com.example.mybatis.entity.User">
    </resultMap>

    <select id="selectLike" parameterType="com.example.mybatis.entity.User" resultMap="getUser">
        select
            id,
            name
        from user
        where 1=1 and
            name like "%"#{user.name}"%"
    </select>

    <select id="selectEqualTo" parameterType="com.example.mybatis.entity.User" resultMap="getUser">
        select
            id,
            name
        from user
        where 1=1 and
            name = #{user.name}
    </select>
</mapper>